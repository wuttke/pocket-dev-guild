# Optional: which agent binary to invoke and how to pass the prompt.
# Defaults shown below.
# agent_binary: auggie
# agent_prompt_param: --print

# Optional: MongoDB connection URL for job, conversation, and repository persistence.
# If not specified, uses in-memory stores (data lost on restart).
# When specified, repositories are stored in the database instead of this config file.
#
# Examples:
#   Local without auth: mongodb://localhost:27017/pocket_dev_guild
#   With auth:          mongodb://username:password@localhost:27017/pocket_dev_guild
#   Remote MongoDB:     mongodb://user:pass@mongo.example.com:27017/pocket_dev_guild
#
# mongodb_url: mongodb://localhost:27017/pocket_dev_guild

# DEPRECATED: The repos section is no longer used. Repositories are now managed
# via the API endpoints:
#   - POST /repos - Register an existing repository on disk
#   - POST /repos/clone - Clone a repository from a URL
#
# For migration instructions, see README.md
#
# repos:
#   - id: example
#     name: example
#     path: /absolute/path/to/example-repo
